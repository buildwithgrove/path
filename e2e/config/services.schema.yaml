# This schema file may be used to validate the services config files using the `yaml-language-server` VSCode extension.
# See: https://marketplace.visualstudio.com/items?itemName=redhat.vscode-yaml

$schema: "http://json-schema.org/draft-07/schema#"
title: "PATH Services Configuration Schema"
description: "PATH Services Configuration YAML: this file is used to configure services for end-to-end tests for PATH gateways"
type: object
additionalProperties: false

definitions:
  service_params:
    description: "Service parameters for test requests"
    type: object
    additionalProperties: false
    properties:
      contract_address:
        description: "Contract/account address for blockchain calls (supports EVM, Harmony, and Solana formats)"
        type: string
        pattern: "^(0x[a-fA-F0-9]{40}|one[a-z0-9]{39}|[1-9A-HJ-NP-Za-km-z]{32,44})$"
        examples: [
          "0x28C6c06298d514Db089934071355E5743bf21d60", 
          "one19senwle0ezp3he6ed9xkc7zeg5rs94r0ecpp0a",
          "FxteHmLwG9nk1eL4pjNve3Eub2goGkkz6g6TbvdmW46a"
        ]
      call_data:
        description: "Call data for eth_call"
        type: string
        pattern: "^0x[a-fA-F0-9]*$"
        examples: ["0x18160ddd"]
      contract_start_block:
        description: "Minimum block number for archival tests - ensures contract address has balance or transactions"
        type: integer
        minimum: 0
      transaction_hash:
        description: "Transaction hash/signature for receipt/transaction queries (supports EVM and Solana formats)"
        type: string
        pattern: "^(0x[a-fA-F0-9]{64}|[1-9A-HJ-NP-Za-km-z]{88})$"
        examples: [
          "0xfeccd627b5b391d04fe45055873de3b2c0b4302d52e96bd41d5f0019a704165f",
          "4XqrV7zHtv8mHZqsPxueeYc1NodL9nxkdeZJYPtAuw67LszjdYxVgHwUJgBVubuE63ZCToBawe22c8DAAuRdipKs"
        ]

  service:
    description: "Service definition for protocols"
    type: object
    additionalProperties: false
    required:
      - name
      - service_id
      - service_type
    properties:
      name:
        description: "Name of the service"
        type: string
        examples: ["Morse - F00C (Ethereum) Test", "Shannon - Ethereum Test"]
      service_id:
        description: "Service ID to test - identifies the specific blockchain service"
        type: string
        examples: ["F00C", "anvil", "eth", "arb_one"]
      service_type:
        description: "Type of service to test (evm, cometbft, solana)"
        type: string
        enum: ["evm", "cometbft", "solana"]
        examples: ["evm", "cometbft", "solana"]
      archival:
        description: "Whether this is an archival test (historical data access)"
        type: boolean
      service_params:
        $ref: "#/definitions/service_params"

properties:
  services:
    description: "Array of services to run the tests against"
    type: array
    items:
      $ref: "#/definitions/service"
