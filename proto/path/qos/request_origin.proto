syntax = "proto3";
package path.qos;

option go_package = "github.com/buildwithgrove/path/observation/qos";

// RequestOrigin enumerates possible request origins:
// - Organic: the request was sent to PATH by a user
// - Synthetic: the request was built by the QoS service to collect data points on an endpoint.
enum RequestOrigin {
  REQUEST_ORIGIN_UNSPECIFIED = 0;
  REQUEST_ORIGIN_ORGANIC = 1; // Organic request, i.e. sent by user.
  REQUEST_ORIGIN_SYNTHETIC = 2; // Synthetic request, i.e. generated by the QoS Service.
}
