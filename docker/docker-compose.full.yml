# This docker compose file is used to run Path in a local development environment,
# as well as all the dependencies needed to use the user data features:
# - a Postgres database
# - a Redis instance
services:
  path_gateway:
    container_name: path_gateway
    build:
      context: ..
      args:
        IMAGE_TAG: ${IMAGE_TAG:-development}
    ports:
      # TODO_IMPROVE: Update the port to match the actual value defined in the config yaml
      - "3000:3000"
    volumes:
      - ./cmd/.config.yaml:/app/.config.yaml
    command: ["./path"]
    depends_on:
      - db
      - redis
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/healthz"]
      interval: 5s
      timeout: 10s
      retries: 5

  # This DB container is provided for local development. It is not recommended
  # for production use. Operator should set up their own secure DB instance.
  db:
    image: postgres:latest
    container_name: db
    restart: always
    ports:
      - 5432:5432
    volumes:
      # Initializes the database with the minimum required tables from the base schema.
      - ../db/driver/sqlc/schema.sql:/docker-entrypoint-initdb.d/init.sql
      # For local development, persist the database data in a .gitignore'd tmp directory.
      - ./tmp/db:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: pgpassword
      POSTGRES_DB: postgres
    healthcheck:
      test: pg_isready -U postgres
      interval: 5s
      retries: 3

  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      # For local development, persist the redis data in a .gitignore'd tmp directory.
      - ./tmp/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 10s
      retries: 5
